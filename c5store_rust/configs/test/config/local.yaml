example:
  foo: BAR
  junk:
    .provider: resources
    path: "example.json"
    format: "json"
  secret:
    .provider: resources
    path: "some_secret.yaml"
    format: "yaml"
  test:
    and: 1 # Override base value
bill:
  barr: AG
  bullshit:
    .provider: resources
    path: "pub_data"
list_of_items:
- "Does"
- "This"
- "work?"
database:
  host: db.local.com  # Override common host
  port: 5433         # Override common port
  user: local_user